version: '3.8'

services:
  backend:
    build: .
    container_name: helpdesk-backend
    ports:
      - "5000:3001"

    env_file:
      - .env

    extra_hosts:
      - "host.docker.internal:host-gateway"